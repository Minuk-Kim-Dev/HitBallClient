using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LoginController : MonoBehaviour
{
   /* void Logininfo(char StudentNumber, char StudentName)
    {
        if(StudentNumber && StudentNumber) // in Database;
        {
            // 메인 UI에서 게임 시작 화면으로 넘기기
        }
        else
        {
            // 로그인 실패 관련 패널 띄우기 -> 이름 LoginFailPanel
        }
    } */
}
